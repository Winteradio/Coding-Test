#include <stdio.h>
#include <algorithm>
#include <vector>

int main()
{
	int Num, size, Temp;
	scanf("%d", &Num);
	int NumArray[10] = {0};
	vector<int> Result;
	
	while( Num > 10 )
	{
		Result.push_back( Num % 10 );	
	}
	
	Result.sort();
	size = Result.size();
	for ( int )
	
}
